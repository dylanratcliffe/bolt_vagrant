{
  "puppet_task_version": 1,
  "supports_noop": false,
  "description": "Returns inventory from Vagrant for use in Bolt",
  "parameters": {
    "vagrant_dir": {
      "description": "Location of the directory containing the Vagrantfile. Defaults to the current directory.",
      "type": "Optional[String]"
    },
    "winrm_regex": {
      "description": "Machines that match this will be connected to using winrm. Otherwise SSH will be used.",
      "type": "Optional[Pattern]"
    },
    "match": {
      "description": "Machines that match this will be included in the inventory.",
      "type": "Optional[Pattern]"
    }
  }
}
